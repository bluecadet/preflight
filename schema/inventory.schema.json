{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://preflight.dev/schema/inventory.schema.json",
  "title": "Inventory",
  "description": "Schema for embedded preflight inventory blocks",
  "$ref": "#/$defs/inventory",
  "$defs": {
    "inventory": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "vars": {
          "type": "object",
          "description": "Inventory-wide variables applied before group and host variables"
        },
        "groups": {
          "type": "object",
          "description": "Optional group metadata referenced by host group labels",
          "additionalProperties": {
            "$ref": "#/$defs/group"
          }
        },
        "hosts": {
          "type": "array",
          "description": "Primary host list",
          "items": {
            "$ref": "#/$defs/host"
          }
        }
      }
    },
    "group": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "vars": {
          "type": "object",
          "description": "Variables applied to hosts that reference this group"
        }
      }
    },
    "host": {
      "type": "object",
      "required": ["name"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Unique host identifier"
        },
        "address": {
          "type": "string",
          "description": "IP address or hostname"
        },
        "transport": {
          "type": "string",
          "enum": ["winrm", "ssh", "local"],
          "description": "Connection transport. Defaults to \"ssh\" when omitted."
        },
        "platform": {
          "$ref": "#/$defs/platform"
        },
        "port": {
          "type": "integer",
          "description": "Connection port (defaults vary by transport: SSH=22, WinRM=5985/5986)"
        },
        "username": {
          "type": "string",
          "description": "Username for authentication"
        },
        "password": {
          "type": "string",
          "description": "Password for authentication"
        },
        "private_key": {
          "type": "string",
          "description": "Path to SSH private key file"
        },
        "private_key_passphrase": {
          "type": "string",
          "description": "Passphrase for an encrypted SSH private_key"
        },
        "https": {
          "type": "boolean",
          "description": "Use HTTPS/TLS for WinRM connection",
          "default": false
        },
        "groups": {
          "type": "array",
          "description": "Group labels this host belongs to, applied in order for variable precedence",
          "items": { "type": "string" }
        },
        "vars": {
          "type": "object",
          "description": "Host-specific variable overrides"
        },
        "known_hosts_file": {
          "type": "string",
          "description": "Path to a known_hosts file for SSH host key verification. Defaults to known_hosts under the default SSH key directory (normally ~/.ssh/known_hosts) when omitted. Verification behavior is governed by host_key_policy (default accept-new)."
        },
        "host_key_policy": {
          "type": "string",
          "enum": ["accept-new", "strict", "insecure"],
          "description": "SSH host-key verification policy. accept-new (default) trusts unknown hosts on first connect and pins them; strict requires the host to already be in known_hosts_file; insecure disables verification entirely."
        },
        "host_key_algorithms": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Restrict accepted SSH host key algorithms during handshake"
        },
        "timeout": {
          "type": "string",
          "description": "Connection/handshake timeout as a Go duration string (e.g. \"30s\", \"1m\") for SSH and WinRM. Defaults to 30s for SSH when unset."
        },
        "jump": {
          "type": "object",
          "description": "Optional single-hop SSH bastion to dial through before reaching this host. The jump host has its own independent authentication and host-key policy.",
          "required": ["address"],
          "additionalProperties": false,
          "properties": {
            "address": {
              "type": "string",
              "description": "IP address or hostname of the jump host"
            },
            "port": {
              "type": "integer",
              "description": "Jump host SSH port. Defaults to 22."
            },
            "username": {
              "type": "string",
              "description": "Username for jump host authentication"
            },
            "password": {
              "type": "string",
              "description": "Password for jump host authentication"
            },
            "private_key": {
              "type": "string",
              "description": "SSH private key value, path, or secret reference for the jump host"
            },
            "private_key_passphrase": {
              "type": "string",
              "description": "Passphrase for an encrypted jump host private_key"
            },
            "known_hosts_file": {
              "type": "string",
              "description": "Path to a known_hosts file for jump host key verification. Defaults to known_hosts under the default SSH key directory (normally ~/.ssh/known_hosts) when omitted. Verification behavior is governed by the jump host_key_policy (default accept-new)."
            },
            "host_key_policy": {
              "type": "string",
              "enum": ["accept-new", "strict", "insecure"],
              "description": "SSH host-key verification policy for the jump host. accept-new (default) trusts unknown hosts on first connect and pins them; strict requires the jump host to already be in known_hosts_file; insecure disables verification entirely."
            }
          }
        }
      }
    },
    "platform": {
      "type": "object",
      "description": "Declared destination platform used for connection-free bundle staging",
      "required": ["os", "arch"],
      "additionalProperties": false,
      "properties": {
        "os": {
          "type": "string",
          "enum": ["windows", "linux", "darwin"],
          "description": "Destination operating system family"
        },
        "arch": {
          "type": "string",
          "enum": ["amd64", "arm64"],
          "description": "Destination CPU architecture"
        }
      }
    }
  }
}
